export const deviceCache = {
  set: (id: string, record: unknown): void => {
    void id;
    void record;
  },
  createOrUpdate: async (id: string, record: unknown): Promise<boolean> => {
    void id;
    void record;
    return true;
  },
};
